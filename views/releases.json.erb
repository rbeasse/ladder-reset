<%= game_releases.map { |release|
  {
    name:            release['name'],
    title:           release['title'],
    time:            release['time'],
    game_class:      release['name'].downcase.gsub(/[^a-z0-9]+/, '-'),
    patch_notes_url: release['patch_notes_url']
  }
}.to_json %>
